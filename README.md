# ReplyNodes Go SDK

The ReplyNodes Go SDK provides typed access to the public ReplyNodes API.

This bootstrap commit reserves the public repository for the generated client
and its handwritten Go API layer. Implementation follows in the feature branch
for issue #296.
